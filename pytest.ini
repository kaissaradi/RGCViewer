[pytest]
testpaths = tests
qt_api = pyqt5
markers =
    performance: performance and benchmark tests
    stress: stress and memory lifecycle tests
    profiling: manual profiling helpers, normally skipped
    visual: visual regression tests
    slow: slow tests that are skipped or selected explicitly during routine runs
filterwarnings =
    ignore::DeprecationWarning
