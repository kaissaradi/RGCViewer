# PLAN.md — current snapshot

Read `docs/AGENTS.md` before this file. This file is the pickup point.

Last updated: 2026-09-25. Branch: `dev-testing`. Testers: `beta` (Q35).

## Active queue (2026-09 sweep)

This is the current work. Start here.

Sources: reports from two testers, the 2026-08-10 lab meeting,
and a code audit on 2026-09-24. `main` is production: `install.sh` updates
every user with `git pull --ff-only` on `main`. Work on `dev-testing`. Commit
each fix separately. Do not push or merge to `main` unless the user asks.
Testers run the `beta` channel: after CI passes on a
`dev-testing` push, fast-forward `beta` to that commit (Q35).

Verify each GUI fix on real data with `tools/gui_harness.py` (see §6). The
unit suite is weak (see defect "Older pytest failures"). A green suite is not
proof. Add one focused test per fix that fails on the old code.

Status: `done` = fixed and verified; `open` = not started; `wip` = started.

| # | Item | Source | Status | Notes |
|---|---|---|---|---|
| Q1 | STA tab does not redraw on first select of an uncached cell | Tester | done | Cause: with a raw file loaded, `_process_selection` waited for `FeatureWorker`, and `on_features_ready` redraws only EI/Waveforms/Standard. Harness `sta_refresh` with `HARNESS_DAT`: 0/12 drew before, 12/12 after. Test `test_selection_draws_active_tab.py`. |
| Q2 | STA image rescales every frame; gray must stay gray | Lab meeting | done | One symmetric scale per movie (±max|STA|), 0 = mid-gray, ImageItem levels fixed. Harness `sta_modes`: image median 0.50 on 3 cells. Test `test_sta_display.py`. |
| Q3 | STA heatmap (spatial) and space-time view | User | done | Display combo: Stimulus / Heatmap (dominant channel, CET-D1, colorbar) / Space–time (x–t and y–t through the fit centre, else the peak pixel). Harness `sta_modes` screenshots. |
| Q4 | STA panel keeps the previous cell's movie after a failed or missing STA | Audit | done | `_clear_all` drops the movie and stops the timer. Harness `sta_modes`: movie dropped, timer stopped. |
| Q5 | STA read blocks the GUI thread up to 8 s on a cold CIFS read | Audit | done | `LazySTADict.cached()` (RAM only). `STAPanel.update_view` draws a hit at once; a miss shows "Loading STA…" and reads in a `BackgroundCall`, one read in flight, only the latest request kept, result dropped if the selection or DataManager changed. Harness: `sta_refresh` (raw attached) 12/12 drawn; event-loop max stall over 12 fresh cells 196 ms → 91 ms (idle mount; the multi-second stalls came under warm-up contention, not reproduced here). Test `test_sta_async_read.py`. |
| Q6 | Grating polar plot: no units, no error bars (SD across trials) | Lab meeting | done | Polar title and histogram axis carry the unit (spikes/s); ring values labelled; ±1 SD whiskers on the polar spokes and histogram bars; footnote states n. Falls back to SEM, labelled as SEM, for files without `sd_response`. Harness `grating`. |
| Q7 | DSI/OSI missing from the cluster table | Lab meeting | done | Never existed. New columns DS/OS, DSI, OSI (`attach_grating_columns`), same Auto pick as the Grating tab. Refreshed when the grating batch finishes and 400 ms after the DS/OS slider settles. Harness `grating_table`: 64 DS / 46 OS at 0.30, 47 / 7 at 0.60. Test `test_grating_table_columns.py`. |
| Q8 | Per-direction rasters around the polar plot (replace the 8-slot PSTH grid) | User, lab | done | `polar_raster_view.py`: one raster per direction at its angle, preferred direction highlighted, stimulus shaded. Condition combo (Auto / each condition) picks what stats, rasters and error bars show. Rasters need raw trials (`grating_status == "raw_only"`); analysed-only files show a note. Tests `test_polar_raster_view.py`. |
| Q9 | Grating math: timing from trial 0 only; p-value has no +1 correction; 0°/360° not merged; negative delta-rate enters vector sum; F1 window overruns stimTime | Audit | done | Fixed in `grating_calc.py`; see its Conventions block. `GRATING_SCHEMA_VERSION = 2` recomputes old cache rows once. Old vs new on all 201 cells of 20251212A/data018: no DS/OS change. Tests `test_grating_math.py`. Open question for the user: no multiple-comparison correction across conditions and DSI/OSI. |
| Q10 | First load: grating DS/OS batch is slow | User | done | Math vectorized: 25 → 6.5 ms/cell. Harness `first_load_timeline` on cold 20250918A/kilosort40/data025 (474 clusters, Vision + 53 MB grating file, no caches): whole load to "Physics Cache Ready" 19.2 s; Kilosort read 4.5 s, stimulus manifest 1.2 s, Vision 7.0 s, grating DS/OS batch 2.5 s (474 cells, ~5 ms/cell, including Q33's family-wise shuffles). The batch is no longer the slow step; the CIFS reads are. |
| Q11 | Kilosort + Vision from separate directories builds the physics cache twice or freezes | Tester | done | Freeze: File ▸ Load Vision disabled the window and only the first reveal re-enabled it; `_finalize_dataset_load` now unlocks on every pass. Stale data: physics entries and `ei_corr_dict.pkl` carry `_vision_source`; a different Vision folder drops Vision caches, closes old readers and recomputes (untagged legacy entries are kept). A running warm-up is stopped before a manual attach. Harness `attach_vision` (and with `HARNESS_VISION=.../data017`): window enabled; 324/324 physics rows rebuilt from the new source. Test `test_vision_source_switch.py`. Not done: joining the old warm-up thread (it stops at its next cell). |
| Q12 | `retinanalysis` ignored even when installed | Tester | done; follow-up open | Cause: `install.sh` makes an isolated venv (`include-system-site-packages = false`), so a conda or editable `retinanalysis` is invisible. Also: `load_stim_timing` sets `block_id` / `d_timing` and nothing reads them, so the package has no effect today. Warning lowered to info; README "Optional packages" shows how to install it into `~/.encore/.venv`. User 2026-09-24: `retinanalysis` will be wired to DataJoint later. To do (not started): use DataJoint epoch timing in Encore. |
| Q13 | State from the previous dataset survives a dataset switch | Audit | done | `DataManager.generation` (never reused). Waveforms: PCA cache key and cloud/PCA payloads carry it; `_is_stale` drops a previous run's result for the same cell ID. EI: `_ei_map_cache` valid per (generation, Vision source); `reset_for_new_dataset` drops the array photo (Photo showed the last run's image). `_forget_previous_dataset_views` (in `_release_previous_dataset`) also clears the population caches. Found on the way: the second load raised `'box_aspect' and 'fig_aspect' must be positive` in `umap_panel.reset_for_new_dataset` → `_blit` of a hidden 0×0 canvas, stopping `_on_kilosort_loaded` half way (Law 5); `_blit` in `rf_map_widget` / `trace_stack_widget` now checks `_axes_ready`. Harness `switch_state` (data018 → data016). Tests `test_dataset_switch_state.py`, `test_blit_hidden_canvas.py`. |
| Q14 | Feature Extraction default scatters | User | done | `feature_catalog.DEFAULT_PANELS`: Temporal STA PC1 v PC2, ACG PC1 v PC2, Temporal STA PC1 v ACG PC1, Temporal STA PC1 v RF area (user: area, not diameter), then two `RANDOM_PAIR`s that repeat no other panel. The old empirically ranked defaults are now the first `FALLBACK_PANELS` (used when a run lacks a feature, e.g. no STA). The no-catalogue `_PLOT_META` path is unchanged. Harness `feature_defaults`. Test `test_feature_default_panels.py`. Needed Q34 to show the ACG pairs. |
| Q15 | Editable, remembered plot presets | Tester | done | Feature Extraction: Preset combo + Save as… / Delete on the second axis row (`panels/feature_presets.py`, QSettings keys `feature_extraction/presets` and `/current_preset`). The preset picked last opens next time; "Default" = `DEFAULT_PANELS` (Q14), so the defaults are not lost. Harness `feature_presets` (temporary settings file; the user's settings are not touched): saved preset applied and restored on reopen. Test `test_feature_presets.py`. |
| Q16 | Light mode has dark leftover panels | Tester | done | Harness `light_mode` (share of near-black pixels per tab, raw attached): Waveforms 0.996, every other tab ≤ 0.003. Cause: `waveforms_panel._C` was a fixed dark palette and `restyle_plots` was a no-op. `palette_from_theme` builds `_C` from the theme tokens (mean trace ink, ensemble blue, compare red, per `docs/design/palette.md`) at build time and on a theme switch. After: Waveforms 0.005. Test `test_waveforms_theme.py`. |
| Q17 | Window width cannot shrink on laptops; drag-resize only in full screen | Lab meeting, tester | done | The window minimum (from its layout) was 1399 px: the analysis `QStackedWidget` takes the widest page, and UMAP's feature-weights grid (1079 px) and Contrast's one-row header (~980 px) set it for every tab. UMAP controls now sit in `umap_panel._HScrollArea` (sideways scroll, height = content + bar only when shown); Contrast controls moved to a second row, summary label may clip (full text in tooltip). Window minimum 1399 → 1019 px. Harness `narrow_window` (1050 px: UMAP bar shown; 1800 px: none). Test `test_window_min_width.py`. Remaining widest pages: Contrast 689, Raw 677, STA 629. |
| Q18 | Light mode far too bright; every line, dot, number and word stands out | Tester | done | User 2026-09-25 clarified. Light palette softened (`theme.py`, `docs/design/palette.md`): off-white surface `#FBFAF7` instead of `#FFFFFF`, ink `#26241F`, muted `#77736A` (AA), hairline `#E4E0D6`, data blue `#4A72B8`, mean ink `#3B3934`, new `plot_tick` `#948F85` for axis numbers; light strokes no heavier than dark; RF mosaic outlines 1.0 px at 0.55; selected-RF fill 0.30; overlaid ensembles fade with √(20/n) (≥ 0.06); matplotlib toolbars at 45 % opacity. Dark mode unchanged. Harness `light_mode` (data022) screenshots. |
| Q19 | Loading indicator wrong on dataset reload | Lab meeting | done | Reload and switch of warm runs were fine. Bug: switch while the first run's caches still build. Its 250 ms progress poll kept running, read the NEW DataManager once its Kilosort files loaded, hid "Loading dataset..." and showed "Physics Cache Ready" ~3 s into the load (and saved the standard cache early). `_release_previous_dataset` now stops the poll; `update_cache_progress` does nothing until `_dataset_revealed`. Harness `switch_mid_warmup` (cold 20251212A/kilosort40/data011 → data018): busy bar held to reveal. Harness `reload_indicator` covers reload/switch. Test `test_reload_indicator.py`. |
| Q20 | Array views rotated 90° against the STA movie / RF mosaic | Tester | done | User 2026-09-25: "lined up to the screen" — the STA and the RF mosaic are in screen coordinates and agree with each other (checked: a cell at the top/left of its STA is at the top/left of the mosaic); everything drawn from electrode positions is in array coordinates. Measured: the affine fit array → RF centre has a −89.2° / −88.1° / −83.4° rotation, no mirror (20260220A/data022, 20260529A/data017, 20260715A/data007-010). `vision_sort_check` now records the fit's nearest quarter turn / mirror (`nearest_quarter_turn`, `SortCheck.screen_turn`, only when the pairing passes). `src/gui/array_orientation.py` turns positions for drawing only (EI panel incl. the array photo via an Affine2D, spatial template, cell tracer); analysis and array calibration keep raw coordinates. Array ▸ Align Array Views to the Screen (on by default, QSettings `view/align_array_to_screen`). Harness `array_alignment` on data022: turn (0, 1, −1, 0) = 90° clockwise; after the turn the refit needs no turn (553 cells); EI screenshots off/on vs the same cell's STA. Runs whose Vision files fail the pairing (data018, data025) are not turned. Test `test_array_orientation.py`. |
| Q21 | Mosaic click-to-select fails when the table view is active | Audit | done | `_select_cluster_in_table` and the tree→table sync read a `_data` attribute the model does not have; both now use `_select_table_cluster_id`. `_is_syncing` is released in `finally`. Harness `selection_sync`. |
| Q22 | Small bugs | Audit | done | `summary_tab` slot → now redraws the EI panel; EI panel says why it is empty with no Vision EI and no raw file; `electrode_map` enum `or` fixed (set is unused today); classification save/load go through `get_vision_id_for_cluster` / `get_cluster_id_for_vision`; a failed CCG says so in the plot title. NEW: `src/gui/crash_guard.py` — PyQt6 aborts (SIGABRT, exit 134) on any exception in a slot; the guard logs to `~/.encore/logs/errors.log` and the status bar instead. Tests `test_crash_guard_and_small_fixes.py`. |
| Q23 | Population spike-rate plots like Vision | Tester | done | `draw_population_fr_panel` (firing rate over the recording, every cell of the group overlaid, mean on top) existed but no `pop_fr_canvas` was ever built. Now the 4th pane of the population view, redrawn with the others, restyled on theme switch, part of `population_group_plots_cached`. Harness `population_fr`: 113 traces for a 116-cell group. User 2026-09-25: "the firing rate is good". |
| Q24 | Classification file structure matches Vision | Tester | wip | File ▸ Save wrote `id path/` (no `All/`, one space); both exports now share `vision_classification_lines` → `id  All/path/`, the format of `data017.classificationYT.txt`. Ctrl+S now writes the class into the `.params` itself (Q31), which is what Vision shows. The `.txt` loader cut names at the first space (`All/ON/brisk transient/` → `ON/brisk`); fixed (`parse_classification_text`). Still ask the tester what else "match Vision" means. |
| Q25 | Tooltip for "RF short vs long" | Tester | done | UMAP checkbox "RF Diameter (long + short)" has a tooltip (`umap_panel.FEATURE_TOOLTIPS`); Feature Extraction descriptions say 2σ along the longer / shorter fitted axis, in stixels. The no-catalogue panels said "RF Diameter (µm)"; the values are stixels (no conversion exists), now labelled so. Test `test_rf_tooltips.py`. |
| Q26 | Vision fits at the lower bound (σx = σy = 1.00) show as real fits | Audit | done | Not a bound: vision7 `ImageFrame.fit` starts σ on a 1..3 grid, so exactly (1, 1) is an unmoved first start. Counts: 23/303 (20251212A/data018), 25/578 (20260220A/data022), 351/584 (20260529A/data017). `rf_geometry.fit_is_unmoved`: `rf_fit_from_stafit` / `rf_fit_from_params` return None (no ellipse, not in the mosaic); physics keeps the 0.0 no-fit sentinel (UMAP / features no longer see a fake π area); STA metrics show "no fit"; saved physics rows with area exactly π recompute once. Test `test_unmoved_rf_fit.py`. |
| Q27 | Infra: CI, pinned dependencies, installer preflight (dirty tree, branch, venv Python), prune the unit suite | User | done | CI: `.github/workflows/tests.yml` (unit suite, Python 3.10 + 3.13, offscreen; not run yet: needs a push). Deps: upper bounds at the next major in `pyproject.toml` / `requirements.txt`; torch removed (only `compute_ei` used it; `np.partition` gives the same lower median, test compares with torch). Installer: `install.sh` stops with advice on local changes or a non-`main` branch in `~/.encore`, rebuilds a venv whose Python is gone (`preflight_checkout`, `venv_is_usable`; test `test_installer_preflight.py`). `install.ps1` unchanged (no PowerShell here to test). Checked in a fresh Python 3.13 venv with the new requirements: unit suite green; harness `grating_table`, `params_save` same results as 3.10. pandas-4 deprecation (`infer_objects(copy=False)`) removed. `CLAUDE.md` and `environment.yml` now exist. Pruned 2026-09-25: read-only Codex audit of ~430 tests, each verdict re-checked; 9 removed that never called Encore code (recomputed maths in the test, mock attributes, constants, import-of-removed-code, subclass checks); kept crash regressions. Suite 499 passed, 2 skipped. |
| Q28 | UMAP does not separate RGC types well | User | measured; causes fixed | Harness `umap_eval`: named Vision types (≥ 8 cells) from the run's .params, leave-one-out 5-NN accuracy in the feature space and in a 2D UMAP of all cells. Matched runs, default features (temporal STA + ACG + RF diameter): 20260220A/data022 5 types, 118 cells: features 0.93, UMAP 0.92 (chance 0.29); 20260227A/data005 5 types, 137 cells: 0.97 / 0.90 (chance 0.31). ACG alone 0.92 / 0.92; temporal alone 0.84 / 0.93; RF diameter alone 0.43 / 0.54. The pipeline separates labelled types when its inputs are right. Why it looked broken: (1) Q34, ACG absent on every warm open; (2) Q26, fake π RF areas; (3) Q32, runs such as data018 whose STAs belong to other cells; (4) the run gate checked physics rows only, so a UMAP started before the spike-plot pass silently had no ACG block. Now `umap_inputs_waiting` also waits for the ACG pass when ACG is selected. Caveat: the labels were probably made with similar features, so these scores flatter it. Test `test_umap_gate.py`. |
| Q29 | Windows / plots overlay each other when switching tabs | User | done | Cause: `_finalize_dataset_load` (and the Vision-only load) called `sta_panel.show()` on a `QTabWidget` page; a page shown by hand stays visible over the current tab. Harness `tab_overlay` (27 switches, raw attached): 310 widgets of the STA page visible on other tabs before, 0 after. Now `callbacks.set_tab_available` enables/disables the tab instead; never `show()`/`hide()` a tab page. Tests `test_tab_overlay.py`; `test_vision_loaded_no_modal_gate` asserted the buggy call and now asserts the tab is enabled. |
| Q30 | Opening a dataset "messes up" the `.params`; Vision cannot open it | User | parked (user 2026-09-25) | Encore never wrote `.params` (no writer in any commit; `ParametersFileReader` opens `rb`). `data018.params` kept its 2026-03-26 mtime through ~20 opens today. Scan of all 413 `/mnt/lab/Array-data/sorted/*/*/data*/*.params` with a Vision-equivalent parser: 411 good; 2 damaged, neither with Encore's `*.ei_index.pkl` marker: `20260213A/kilosort25/data017` (zero bytes at row 598) and `20260305A/kilosort2_5/data011` (seek table disagrees with the data from row 11: half-saved). Vision's own save (`ParametersFile.flush`) rewrites the file in place, no temp file, no truncate, seek table last, and Vision opens `.params` read-write even to read it. An interrupted or concurrent Vision save gives exactly this damage. Ask the user: which dataset, what does Vision say? |
| Q31 | Ctrl+S saves the classification into the `.params` | User | done | `src/analysis/params_classification.py`: strict Vision-equivalent parse; rewrites only `classID` cells; temp file + fsync + read-back + `<name>.params.bak` + atomic rename; refuses damaged files and files changed since review (stamp). File ▸ Save Classification to Vision .params (Ctrl+S) and File ▸ Load Classification from Vision .params. Dialog on the first save per session, after an outside save, when cells lose a class, or when Q32 flags the files. Noisy moved Ctrl+S → Ctrl+Shift+N (`ux_ui_redesign.md` AC18). Checked: 411/411 lab files rebuild byte-identical with no change; Vision.jar reads written files; harness `params_save` on data018 and 20260220A/data022 (scratch copy; lab file stamp unchanged). Tests `test_params_classification.py`, `test_params_save_gui.py`. Not tested: writing on the CIFS mount itself (same mkstemp + `os.replace` path as `ei_index_cache`, which works there). |
| Q32 | Vision `.sta`/`.params` made from a different sort than the loaded Kilosort run | Found 2026-09-24 | done | Law 1 pairing is only right if the Vision files came from this sort. `src/analysis/vision_sort_check.py` fits RF centre (x0, y0) against array position (`x_um`, `y_um`) over paired cells; robust R² < 0.15 = mismatch. Matched folders 0.38–0.86; ids shuffled < 0.01; **20251212A/kilosort40/data018: 0.03** (its `.sta` is dated 2025-12-18, before the 2026-01-27 sort; `.neurons` matches the sort 208/208). So data018's STAs, RF fits and Vision classes are on other cells. Shown as a permanent status-bar label (no modal; see `_on_vision_loaded` note), in the Ctrl+S dialog, and as a warning strip on the STA panel (user 2026-09-24: "do whatever's better" → mark, do not hide; the check is a heuristic). Second run flagged 2026-09-24: **20250918A/kilosort40/data025** (R² 0.01; STAs good, median peak/RMS 10.9; same dates for sort, .neurons, .sta). Cross-check with the Vision EI, which the app does not use for this (RF vs EI centre / EI centre vs Kilosort position / RF vs Kilosort, template centroids): matched 20260220A/data022 0.88 / 0.98 / 0.92; data025 0.89 / 0.22 / 0.15 (Vision files agree with each other, not with the sort's clusters); data018 0.22 / 0.42 / 0.08 (its April .ei partly matches the sort, its Dec .sta/.params do not). Open for the lab: regenerate data018's and data025's Vision files from their sorts. **Extended 2026-09-25:** a doubtful band (robust R² 0.15–0.30: every matched folder so far ≥ 0.38; 20260715A/data003 0.22 now warns "Weak Vision/sort pairing"), and the run's median STA peak/RMS in the check: below 5.5 the warning names noise STAs as the cause instead of another sort. **New data findings for the lab:** 20260721A/kilosort25 data006 and data007 — white-noise STAs are noise (median peak/RMS 4.1 / 4.5, none above 6; good runs 7–12), RF vs EI R² 0.01: the STA analysis probably used the wrong movie / seed / frame timing (no ID shift −3…+3 explains it); 20260715A/kilosort25/data003 — doubtful (RF follows its own EIs weakly, R² 0.22, and disagrees with the same cells in data010). Test `test_vision_sort_check.py`; harness `sort_check`. |
| Q34 | ACG block empty on a warm open (Feature Extraction, UMAP) | Found 2026-09-24 | done | ACG reached `feature_cache` only from `_compute_standard_plots`. A warm open restores `standard_plot_cache.pkl` and computes nothing, and a physics row saved without `acg` counted as fresh forever. 20251212A/data018: `acg_norm` in the standard cache for 324/324 cells, `acg` in physics for 0/324; the catalogue had no ACG PCs. `get_cell_physics` now copies it across (`_backfill_acg`, locks not nested). Harness `feature_defaults`: 18 → 21 features. Test `test_physics_acg_backfill.py`. |
| Q33 | DSI/OSI significance: no multiple-comparison correction across conditions | Audit, Q9 | done | User 2026-09-24: one test across conditions. `grating_calc._fill_pvalues`: every DSOS condition is shuffled when any reaches the floor; `family_pvalues` = max statistic over conditions per shuffle → `DSI_pvalue_fw` / `OSI_pvalue_fw`; `gate_pvalue` feeds `select_best_dsos_condition` (files without trials: per-condition p × n conditions). `GRATING_SCHEMA_VERSION = 3` recomputes cached rows once. Simulated untuned cells, 2 conditions: 9.75 % → 5 % pass. 20251212A/data018 (208 cells): DS 64 / OS 47 / none 97 before and after; 2 cells swap OS↔DS; 6.6 ms/cell. Grating tab shows the gate p. Test `test_grating_family_pvalue.py`. |
| Q35 | Beta channel: testers get `dev-testing` work before `main` | User | done | `install.sh` / `install.ps1`: `ENCORE_BRANCH=beta` (or `main`) switches an install; without it an install stays on its channel (`resolve_branch` / `Resolve-Branch`, `update_checkout` / `Update-Checkout`); a hand-moved branch or local edits still stop with advice. `install.ps1` now has the dirty-tree preflight too. Testers run the script from the `beta` URL (README "Beta channel"); `main`'s old script does not know `ENCORE_BRANCH`. `src/build_info.py`: File ▸ About Encore and each `errors.log` entry name the build (`beta @ <commit> (<date>)`). `beta` moves only forward: fast-forward it to a `dev-testing` commit whose CI passed. Tests `test_installer_preflight.py` (bash; PowerShell when `pwsh` is on PATH, as on GitHub runners), `test_build_info.py`. |

Parked, low priority: two datasets side by side.

User 2026-09-25: no lab-wide audit of Vision/sort pairing (the two Q32 runs
are one-offs).

## Next (user order, 2026-09-25)

User 2026-09-25 approved ideas Q39–Q44. "Cell against its type" already exists: the population panel overlays the selected cell on its group.

| # | Item | Notes |
|---|---|---|
| Q36 | Suggested cell classes from labelled runs (+ review queue) | **Built 2026-09-25.** User: "the killer idea". Review queue: least-confident first, one key to accept or pick from the top 3 (user: "decent"; faster workflow needed, see Q39). Propose a class and a confidence for each cell from runs the lab already classified; the user confirms; Ctrl+S (Q31) writes it. Evidence so far: Q28 leave-one-out 5-NN 0.92 / 0.97 within a run, chance ~0.3, but the labels were made from similar features. First step: measure train-on-one-prep, test-on-another accuracy offline before any GUI. **Measured 2026-09-25** (offline, `named_cells_v2`, 2,767 certain labels, 31 preps, leave-one-prep-out, chance 0.20): random forest on run-scaled time course + ACG (20 log bins 0.5–300 ms) + run-relative RF size: balanced accuracy 0.90 (per prep median 0.94, min 0.67); ACG + polarity alone 0.90; time course alone 0.73–0.82; a random split sharing preps flatters (0.977). ON/OFF never confused; OFF brisk transient weakest (77 % recall). Polarity by rule (sign of the first sample ≥ half max, scanning back from the spike) matched 100 % of labels. RF confidence is usable: reviewing the least-confident 30 % catches 207 of 264 errors. Types outside the 5 classes get confident wrong answers → a novelty / quality gate is needed. Time base: all `.sta` headers say 8.333 ms, but the median peak frame runs from 18 to 29 between runs (stretched, not shifted) — the real frame interval is not in the header; scale by the run's median time-to-peak until it is found. **Label findings (checked):** 20260731A/kilosort25/data004 `.params` has every ON/OFF inverted vs the STA (0/84 agree; its `20260731A.classificationag.txt` and `data004.classification.txt` agree with the STA); 20250912A "OFF brisk transient" (10) look like OFF transient; 20260313A "ON brisk sustained" (15) look like ON brisk transient. Scripts: session scratchpad `q36/` (not in the repo). Next: build it — polarity rule, RF classifier trained at run time from the lab library (cache in `~/.encore`, never commit a model), novelty gate, review queue (Q39 keys), per-run label-check report. **Built:** `analysis/type_features.py` (the measured features; run medians from cells with a real fit), `analysis/type_library.py` (scans `/mnt/lab/Array-data/sorted` sequentially, cache `~/.encore/type_library.npz` keyed by size + mtime; drops uncertain labels, duplicates, runs whose ON/OFF contradict the STA — found 20260731A by itself), `analysis/type_suggest.py` (random forest, polarity by rule, novelty gate: 10th-NN distance beyond the library's 99th percentile → no suggestion), `gui/suggestions.py` (Types tab ▸ Suggest classes / Accept confident; line under the cell list; Ctrl+J next to review, Ctrl+Enter accept; folders named the lab's way, `ON` ▸ `brisk sustained`; bulk accept never moves a classified cell and holds back a cell whose RF would sit on a same-class member, NNND < 1). Checks: the Encore modules leave-one-prep-out on the library (2,767 cells, 31 preps, 46 runs; first build 86 s over CIFS): balanced accuracy 0.922; ≥ 0.8 confidence: 70 % of cells at 97 %; 2 % gated as novel. Harness `suggest` on 20260220A/data022 (its prep left out of training): agrees with 89.8 % of its 118 named cells; 89 % of its 199 'weak' cells get no suggestion (4 % confident); Accept confident moved 52 cells, held back 5; afterwards no class has a close pair (before the mosaic screen, ON brisk sustained had 5). Tests `test_type_suggest.py`. |
| Q37 | Match cells across runs | Was parked (tester request). Not from scratch: File ▸ Map Reference Run already matches cells by EI (`cross_run_matcher.py`, KS-template fallback, RF-position check, JSON sidecar) and borrows the reference run's STA / chirp / grating (`reference_bridge.py`, spec `docs/specs/cross_run_stimulus_bridge.md`, stages 0–4 done). Open: stage 5 (show borrowed curves in the single-cell Chirp / Grating panels) and the lab acceptance on real data, never run. First step: run it on two runs of one prep and measure match quality. **Validated 2026-09-25** (session scratchpad `q37/validate.py`): Map Reference Run's EI matcher finds the same cells. Check: matched pairs' EI somas, and their RF centres from each run's own white-noise STA, compared with an affine fit (any screen offset allowed). 20260529A data017↔data014: 280 matched, somas median 1.0 µm apart (96 % < 30 µm), RF affine R² 0.94 (real fits 0.98); 20260724A data006↔data003: 489 matched, 1.0 µm (97 %), RF R² 0.98 (0.99). Where the RFs disagreed it was the Vision files, not the matcher: 20260715A data003↔data010 somas 15 µm but RF R² 0.20, because data003's own RF vs EI fit is 0.22 (data010 0.50); 20260721A data007↔data006 somas 0.8 µm, RF R² 0.004, both runs' STAs are noise (below). Match IDs are Vision IDs. **Stage 5 (chirp) done 2026-09-25:** a cell with no chirp in this run shows its matched reference cell's chirp in the Chirp tab (`DataManager.get_borrowed_chirp`), with a note naming the reference run, the matched cell and the EI match confidence; the raster shows the reference cell's own trials; the QI is rescored from them like this run's (the reference file's own `quality_index` is on another scale: 217–1953 on 20260715A). Harness `borrowed_chirp` (20260715A data010 ← data007-010): 394 cells mapped, 361 borrow a chirp. Not done: grating — the bridge borrows only analysed grating files and the lab's are raw trials. Test `test_borrowed_chirp.py`. |
| Q38 | Spike rasters when there is no raw file | Done 2026-09-25. The Raw tab is always enabled (also in Vision-only mode); without a raw file it shows `panels/raster_view.SessionRaster`: one row per cell (the selected cell on top, then its group, or all cells; at most 400), the whole recording, stimulus blocks shaded (Q41). Zoomed out each row is a density image recomputed for the visible window (`np.searchsorted` on sorted spike times), scaled to the row's own 99th percentile; below 6 s every spike is a tick. Hover for the ID, click to select. data022 (400 rows, 15.2 M spikes, 1 h): first show 0.64 s, 300 s window 24 ms, 4 s window 4 ms, full hour 52 ms. Harness `raster`. Test `test_raster_view.py`. |
| Q39 | Keyboard workflow | Done 2026-09-25. `src/gui/keymap.py` `BINDINGS` is the one table: window-level shortcuts, the F1 / ? cheat sheet (also File ▸ Keyboard Shortcuts), README's table (test checks they match), key hints in the tree/table context menus. New: Delete / Backspace trash from any focus (was tree/table focus only), Ctrl+M group picker (type to filter, ↑↓, Enter, remembers the last group), Ctrl+Shift+M repeat last move, Ctrl+G new group from selection, F2 rename group, Ctrl+Shift+F Feature Extraction, Ctrl+1…9 / Ctrl+Tab / Ctrl+Shift+Tab tabs (skip disabled), Ctrl+T tree ↔ table, Ctrl+P population pane, Ctrl+O open, Esc leaves search. Fixed: `KeyForwarder` (app-wide filter) ate Space, ←/→ and Ctrl+A/C/X in every text field and acted inside dialogs and open menus; now only keys for the main window's non-text widgets (↑/↓ still move the list from the search bar); parented to the window so it goes with it. Harness `keyboard` (20260220A/data022, raw attached): every key did its job, 1–19 ms each. Tests `test_keymap.py`. Not done: undo. |
| Q40 | Type barcode | Done 2026-09-25. Types tab (`panels/types_panel.py`, `analysis/barcode.py`): one row per cell (STA time course / ACG 0–100 ms / chirp), rows scaled to their own peak, one band per tree folder (Trash and loose cells left out), most typical first (leave-one-out r with the rest of the group). A red tick marks a cell of a *type* group (not a bin such as unclassified / weak / nc) that fits another type's mean better than its own by 0.05, or r < 0.5. Reads cached data only (`DataManager.peek_cell_physics` / `peek_standard_plot_data`), says how many cells are still computing vs have no data (e.g. not in the Vision files), polls every 3 s while shown. Click a row to select; hover for r and the best other type. Harness `types_tab` (20260220A/data022, its Vision classes): built in 0.56 s on first show, forced rebuild 145 ms; STA rows: 0 misfits among the named types; ACG rows: 1. Tests `test_barcode_mosaic.py`, `test_types_panel.py`. |
| Q41 | Stability strip | Done 2026-09-25. Standard tab ▸ firing-rate plot: amplitude on its own right axis in % of the cell's median (cached 1-s means `fr_amp_*`, else binned here; the old overlay drew every spike, 100k+ points, scaled to the largest one), stimulus blocks shaded and labelled, and a verdict in the title ("stable", "fires little during GratingDSOS (8 % of its rate)", "amplitude falls 35 %"). Blocks: `analysis/recording_timeline.py` lays the sort's runs end to end (folder `data007-010` / `data000_data004` / `data022`) with lengths from the stimulus manifest, and shows them only when the last spike falls in the final run. Checked on 20260715A/kilosort25/data007-010: 4 runs sum to 89,560,000 samples, last spike 89,559,992 (Chirp, Contrast, GratingDSOS, SpatialNoise); 3 of 40 cells flagged (e.g. one fires only in the last block). Warm update 10 ms per cell. Harness `stability`. Tests `test_recording_timeline.py`. |
| Q42 | Mosaic atlas with gap finding | Done 2026-09-25 (Types tab, `analysis/mosaic_stats.py`). One tile per type group (named types first; bins skipped), all at one scale (the extent of the fitted RFs). Tile line: median NNND (Ravi 2018: 2·d / (S1 + S2), S = 1-SD radius along the line between centres; ≈ 2 when RFs touch) and close pairs (NNND < 1, red outlines). Grey rings: unclassified cells of the type's polarity (STA sign rule from Q36) inside the type's footprint, NNND ≥ 2.5 from every member, whose time course correlates ≥ 0.8 with the type mean (at most 10). Coverage is computed but not shown: arrays record only some cells (`docs/design/rgc_types.md`). Vectorised (1.7 s → 0.15 s for 731 cells). data022: NNND 2.8–3.6 for the 5 named types, no close pairs. Click an outline or ring to select. |
| Q43 | Retina orientation from axons | EI propagation gives each cell's axon direction; axons run to the optic disc, so all cells together locate it. Check on 20260220A/data022 that the EIs are clean enough first. Goal (user 2026-09-25): with the tissue orientation known, express DS preferred directions in retinal coordinates and pool all DS grating runs across preps. Axons give only the direction to the optic disc; dorsal vs ventral also needs the piece's position in the eye (ask whether the lab records it). **Measured 2026-09-25** (subagent, scratchpad `q43/`, detector frozen after one revision, nulls: permuted, random, edge-aware): axon = electrodes ≥ 2 % of soma amplitude, > 100 µm, lagging 0.1–5 ms; robust position-vs-latency fit. 20260220A/data022 (512 array): axon found in 377 / 496 cells, 59 with R² ≥ 0.7; speed median 0.61 m/s; 85 % point within ±20° of one point (held-out 84 %; edge-aware null 36 %), 0.98 mm from the array centre at bearing 87°; same piece data006: 88 %, point 70 µm from data022's. 20260529A/data017 (519 array, 0.72 × 0.78 mm) and 20260227A/data011: directions agree (R 0.53 / 0.75) but no point resolved. Recommendation: build a bearing readout with a quality gate (R² ≥ 0.7, ≥ 5 axon electrodes, ≥ 150 µm, ≥ 20 cells, beats the edge-aware null), bootstrap interval, disc point only when bounded; bridge one-electrode gaps first.  **Built 2026-09-25:** Array ▸ Find the Optic Disc (`analysis/axon_bearing.py`, `panels/optic_disc_dialog.py`): the measured method ported unchanged, graded verdict disc / direction / none (≥ 20 axons with R² ≥ 0.7; disc only when the held-out fit beats the edge-aware null's 97.5th percentile and the bootstrap distance range is < 0.6 × the distance; direction when the resultant beats the edge-aware null, p < 0.05). The port reproduces the measurement: data022 disc (52, 977) µm, bearing 87°, 85 %, 0.61 m/s; data006 (−2, 933); data017 direction only, 99°. Reads the .ei once in the background (1–6 s), figure in the EI panel's orientation, result kept as `DataManager.axon_bearing` for pooling DS runs (not done yet). Tests `test_axon_bearing.py`; harness `optic_disc`. |
| Q44 | Type atlas from the lab's classified runs | Per class, mean and spread of timecourse and ACG across preps, from `.params` alone (`RedTimeCourse`, `Auto`, `acfBinning`). Also a name harmonizer. Scan 2026-09-25 (all 413 `.params`, read-only): 411 readable; 90 runs from 51 preps have ≥ 20 classified cells (37,231 cells), but 465 distinct class names: case variants (ON/on, UNCLASSIFIED/unclassified/unclasssified), hyphen variants (brisk-sustained), numbered clusters (`nc22`), junk bins (weak, huge, misfit). After lower-casing, hyphen → space and dropping unclassified / nc / junk: 5 named classes seen in ≥ 16 preps — off/transient 713 cells (21 preps), on/brisk sustained 562 (20), off/brisk sustained 514 (22), off/brisk transient 456 (20), on/brisk transient 428 (16), plus the hyphen variants (~230 cells). `src/analysis/class_names.canonical_type` merges every spelling (case, hyphen, underscore, run-together, sub-cluster `nc`, `?` / `maybe` = uncertain): OFF transient 795 (82 uncertain), ON brisk sustained 656 (24), OFF brisk sustained 635 (1), OFF brisk transient 517, ON brisk transient 472, ON transient 26, OFF small transient 13; left out on purpose: `on/brisk-sustained/removed-brisk-sustained` (17), `on/off-center OBS` (9). This is the training set for Q36. Caveat: timecourse frame interval and stixel size differ between runs; convert to ms / µm before comparing. **Viewer done 2026-09-25:** Types tab ▸ Type atlas (`panels/type_atlas.py`, `type_library.atlas_profiles`): per named type the lab-wide mean ± 1 SD of the run-scaled time course and log-binned ACG (all preps), this run's cells in that folder on top, and the literature signature from `docs/design/rgc_types.md`. Uses the library built by Suggest classes (runs it first if needed). data022: 5 types, 17–33 of this run's cells each. |
| Q45 | Split screen: compare a cell with its population or with other cells | PI request (user 2026-09-25). Done 2026-09-25 (`panels/population_compare.py`). The population pane (Ctrl+P) drew the group's time courses / ACGs / firing rates but not the selected cell (only the RF mosaic marked it). Now the selected cell is a red line over the blue wash, and Ctrl+K pins up to 4 more cells (yellow / red, told apart by dash; never ink, the mean is ink), each named in a key; Ctrl+Shift+K clears; pinning opens the pane. Same-group selection moves only the overlay lines (group drawing and caches untouched); a full redraw puts them back. Runs in Tier 2 as soon as the selection settles, before the raw-file features. Harness `population_compare` (20260220A/data022, its Vision classes, ON brisk transient 25 cells): overlay follows a new selection in 0.17 s incl. the 150 ms debounce (1.3–1.7 s when it waited for the features); update alone median 20 ms. Pins clear on a dataset switch. Test `test_population_compare.py`. Not done: pinned cells in the RF mosaic. |
| Q46 | Rethink type discovery: UMAP, features, space-time STA | User 2026-09-25. Design review (Codex, read-only) checked in the code: (1) UMAP clustering runs on the 2-D embedding on purpose (`umap_panel.py` ~1097: the boundaries shown are the ones computed) — clustering a projection; (2) `analysis_core.peak_align_timecourse` rolls each time course so its peak is centred, which removes time-to-peak — the integration time that separates brisk transient from brisk sustained (`docs/design/rgc_types.md`); (3) RF size is in stixels (20–100 per run). Plan: Ravi-style time-course metrics in ms (biphasic index, time-to-zero, time-to-peak) and RF diameter in stixel-size units from `.params`; a space-time STA extractor (rank-1/2 SVD in a physical window, radial centre/surround profile, separability) later; cluster in feature space with a stability check, UMAP for display; validate with leave-one-prep-out and mosaics (Q42). Measure each change (harness `umap_eval`, the Q36 cross-prep test) before changing defaults. **Step 1 done 2026-09-25** (harness `umap_eval`, 5-NN type accuracy, labelled cells, both matched runs; measured after Q48): (a) time courses are no longer peak-aligned; (b) a polarity column (STA sign rule, 2 x the matrix's median column spread; 0 for cells without an STA — NaN let those cells bridge ON and OFF in the UMAP graph: 0.93 / 0.83) is on by default (UMAP checkbox "ON / OFF polarity"). Before → after, 2-D UMAP: 20260220A/data022 0.941 → 0.949, 20260227A/data005 0.839 → 0.985; ON / OFF purity of UMAP neighbours 0.966 / 0.898 → 1.00 / 1.00; feature space 0.932 / 0.978 → 0.941 / 0.985. Tried and not adopted: PCs scaled by PC1's spread instead of each to unit variance (UMAP 0.898 / 0.883: mixed). Known weakness kept: unit-variance PCs inflate weak components (a synthetic 1-D test shows it). Next: Ravi-style time-course metrics, space-time STA features, clustering in feature space. |
| Q47 | Stray `tmpXXXX` files in `ksfiles/` | Found 2026-09-25 | done (new ones); old ones for the user | Nine bare `tmp*` files on the lab share (e.g. five of 8.7 MB in 20260715A/kilosort25/data007-010/ksfiles, 2026-08-14 09:07–09:34): `_save_pickle_with_fallback` used `mkstemp` with no name, and a killed write (crash / SIGKILL) left the temp behind. Now temp files are `<cache>.<random>.encore-tmp`, removed on any exit path (`BaseException`), and `DataManager.sweep_stale_temp_files` removes this app's own leftovers older than a day at each load. The nine old unnamed files are NOT deleted (their names do not prove they are Encore's): `ls /mnt/lab/Array-data/sorted/*/*/*/ksfiles/tmp*`. Test in `test_data_manager_cache.py`. |
| Q48 | Kilosort sample rate read from the wrong key: every time 2/3, every rate 1.5x | Found 2026-09-25 (Q41) | done | `_load_kilosort_params` read `params.get("fs", 30000)`; Kilosort / phy write `sample_rate`, and all 1,000 lab `params.py` say `sample_rate = 20000`, so Encore used 30000 for every Kilosort run: firing rates 1.5x, ISI / ACG lags / recording times 2/3 (a 2 ms ISI shown as 1.33 ms; refractory-violation counts off), EI and raw time axes too. Now `sample_rate`, then `fs`, then 30000 with a warning. `standard_plot_cache.pkl` is stamped with its rate (`SAMPLE_RATE_KEY`); a cache made at another rate — and every unstamped one, all made at 30000 — is dropped once the rate is known (`_check_std_cache_rate`; the cache is restored in `__init__` before params.py is read) and rebuilt in the background; physics rows lose only their copied `acg`. The ISI cache key already held the rate. Found and fixed on the way: the first version re-took the non-re-entrant `_feature_lock` inside the feature-cache restore and froze the load (test `test_restore_path_with_an_old_rate_cache_does_not_hang` fails on it). Checked on 20260220A/data022: recording 3600 s; Encore's ACG vs Vision's own `Auto` (separate time base) over 228 cells: half-max rise lag ratio 1.05 (the bug gives 0.67); the 731-cell cache rebuilt in < 10 s; warm reopen 4.3 s, no rebuild. Tests `test_sample_rate.py`. |
| Q49 | A new user sees an empty, disabled window | User 2026-09-25 ("easy for a new user") | done | `panels/welcome_panel.py`: until a load starts the main area shows what Encore is for, an "Open a run… (Ctrl+O)" button, recent runs as one-click links (`recent_paths.remember_recent` / `recent_runs`, 6, existing folders only; nothing reopens by itself — standing decision 1), three steps (open, look at a cell, classify) and where F1 and About are. Styled from theme tokens (links use the plot blue: 5.6:1 on dark paper; the chrome accent was 2.0:1). A load switches to the analysis view (`show_analysis_view`). Test `test_welcome.py`. Empty stimulus tabs (Chirp, Grating, Contrast) now say why and what would fill them — the file name Encore looks for and where, or that this cell is not in the file — instead of "No chirp data" (`gui/empty_states.py`, checked against `DataManager._ANALYSIS_GLOBS`). Harness `empty_states`. Every tab explains itself: the header "?" button or Shift+F1 opens a short note on what the current tab's plots show and what to look for (`gui/tab_help.py`; on the welcome screen, how to start). A test checks every tab has a note. |
| Q50 | Cell changes froze the GUI up to 2 s with a raw file attached | Found 2026-09-25 (harness `smoke_all`) | done | `_process_selection` called `_cleanup_thread` on the previous FeatureWorker: `wait(2000)` on the GUI thread, then `terminate()`. Now one raw read at a time (`_start_feature_worker`, `_feature_worker_done`): a read in progress finishes, only the latest selected cell is read next, a stale result is discarded as before. Harness `rapid_select` (data022 + raw, 20 cells 150 ms apart): longest stall 2018 → 292 ms, stalls > 250 ms 4 → 1, terminated threads 1 → 0, the last cell has its features. Same run found and fixed: the MEA similarity table indexed a stale row position (`cluster_id_to_idx` from a larger table: cell 793 of 731 rows raised IndexError). Tests `test_feature_worker_queue.py`, `test_data_manager_cache.py`. |
| Q51 | Compare DS grating runs across preps in one tissue frame | User 2026-09-25 ("if we can get tissue alignment we can compare all our DS gratings runs") | done | Array ▸ Compare DS Runs (and a button on the Grating tab): `analysis/ds_pool.py`, `panels/ds_compare_dialog.py`. Per grating run: DS cells by the Grating tab's own test (`grating_calc.select_best_dsos_condition`, computed fresh: DataManager's grating cache is keyed by Kilosort or Vision ID depending on how the run was opened, and the file does not say which); the optic-disc direction from the run's own `.ei` (Q43); the array → screen turn from its own or the nearest white-noise run's `.params` RF centres against its `.ei` somas (`vision_sort_check.check_pairing`). **Direction convention:** the protocol's θ moves the bars toward θ + 180° (open defects), so the dialog uses the motion direction and says so. **Turn gate** robust R² ≥ 0.4, set on a survey of 75 white-noise runs of the grating preps: all 49 at ≥ 0.4 gave a proper rotation (47 the rig's −90°, 2 on 20240820A +90°, so the turn is measured per prep, not assumed); below 0.4 mirrors and identities appear (20251212A/data018, the known other-sort run, 0.33 and a mirror). Every prep (harness `ds_compare`, HARNESS_DS_SCOPE=all, 2026-09-25): 41 grating runs, 7 are bar-width sweeps (one direction), 34 have DS cells (1–145 per run), 14 have both a disc direction and a trusted turn and are pooled in the disc frame (798 cells); first scan 619 s, a second look 19–21 s (file checks on the share; one prep 0.1 s). 20260220A/data023: 127 DS of 454, its own EI puts the disc at (59, 954) µm (data022's: (52, 977)), four lobes 31° off the disc axis. Frames: as the bars moved on the screen, or anticlockwise from the direction to the disc (0° = toward it); "Pieces are dorsal" (on by default: the lab cuts dorsal pieces, user 2026-09-25; S opsin is repressed dorsally, Applebury et al. 2000, PMID 11055434) labels 0° ventral and 180° dorsal. Nasal vs temporal needs the eye. One run per prep is pooled by default (two runs of one piece record the same cells); a recording sorted twice counts once (`prefer_sorter`). One JSON summary per run in its `ksfiles/ds_pool_summary.json`, stamped with its sources. Tests `test_ds_pool.py`; harness `ds_compare` (HARNESS_DS_SCOPE=all for every prep). |

This file lists standing decisions, fragile code, and open defects. It is
not a roadmap. The full UX redesign spec is still parked except the
color / Swiss-header / plot-refresh pass now on this branch. The locked
palette is in `docs/design/palette.md` (warm paper, warm ink never
`#000`, red / yellow / blue). Chrome accent is Bauhaus blue. Plots use
ink + blue + yellow on `surface`. See `docs/specs/ux_ui_redesign.md`.

Do not push unless the user asks.

User check 2026-08-12: load, UMAP defaults, no-STA cells, and EI View combo
are good. Contrast was not checked.

User check 2026-08-24: grating DS/OS on `1212A/data018-` (6 dirs per
`(bw, tf)`, not a 12-dir crossed grid). Grouping, best-run pick, and
population polar without STA are in. Spec:
`docs/specs/grating_dsos_flexible_conditions.md`.

Tree sidebar (2026-08-12): tree is a 3-column ID / Spikes / Ch table.
Folder/file icons and `#3C3C3C` fills are gone. Window open size is
clamped to the usable screen. Sidebar auto-collapse on a narrow window
is still parked.

## 0. Standing decisions

These are also Laws 4–5 and invariant 8 in `docs/AGENTS.md`.

1. Do not reopen the last run at application start. File dialogs still
   remember the last folder.
2. Broken EIs on older kilosort4 conversions are accepted. Do not remap
   a 519-channel EI onto a 512-electrode plot.
3. Do not rewrite the application as HTML.

## 1. Fragile zones

Read the failure column before you change the file.

| What | Failure | Before you change it |
|---|---|---|
| `data_manager.py` | Every panel and cache depends on it. Concurrent writes are timing-dependent. | Rebase. Run the unit suite. |
| `get_cell_physics()` | Vision ID offset lives here. Wrong key returns the previous cell's STA. | Run `test_get_cell_physics_vision_id_offset` (both branches). |
| `build_cluster_dataframe()` | One `np.unique` scan builds `_spk_unique_cls` and `_counts`. A second scan breaks counts and doubles load time. | Do not add `np.unique` or `np.argsort` on the full spike arrays here. |
| `update_cluster_views()` / `_process_selection()` | Tier 1 / Tier 2 boundary. Heavy work in Tier 1 freezes keypress scroll. | Classify each new call as Tier 1 or Tier 2. See AGENTS.md Law 2. |
| `LazySTADict` | Thread-local readers. Handle leaks if extra instances are created. | Run `test_lazy_sta_dict_reads_are_concurrent` and `test_lazy_sta_dict_cache_is_thread_safe`. |
| `_save_pickle_with_fallback()` | Atomic write via tempfile + `os.replace`. A direct dump can leave a truncated file. | Do not simplify. |
| `_compute_ei_correlations_if_needed()` | `is_vision_only` guard stops a RAM-exhausting correlation matrix. | Do not remove the guard. |
| `get_cluster_spike_indices()` | Callers assume O(1). `np.where` on the full arrays is O(N). | Do not bypass. |
| `theme.py` | QSS and `restyle_plots` use the token dict. A renamed key breaks theme toggle. | Toggle theme. Check every tab. |
| `_setup_style(colors)` | Builds the stylesheet from tokens. Hard-coded hex in panels breaks light mode. | Grep `panels/` for hex literals. |
| `live_selectors._axes_ready` | A 0×0 hidden canvas makes `RectangleSelector` raise `ValueError`. | Run `tests/unit/test_live_selectors.py`. |
| `visionloader.EIReader` | Payload width comes from the `.ei` file. A globals-based stride invents cell IDs. | Run `tests/unit/test_vision_load_robustness.py`. |
| `_apply_ei_updates()` | `cluster_df` is main-thread only. `max_dup_r` is float64. | Run `test_apply_ei_updates_keeps_max_dup_r_as_float`. |
| `EIPanel._redraw_current_view()` | Shared handlers that call `_draw_heatmap_frame` steal the View combo. | Run `tests/unit/test_ei_panel_view.py`. |
| `build_feature_matrix()` | Missing STA/grating/chirp/RF rows are NaN. Filling them with 0 rebuilds the fake "no STA" cluster. | Run `tests/unit/test_dynamic_clustering.py`. Do not switch UMAP to sklearn `nan_euclidean` (MCAR scale-up). |
| `pop_mosaic_canvas._pop_plot_state` | A `None` sentinel makes `hasattr` true and `.get` throw. Nested `fig.clear` / `add_axes` during selection paints recursively. | Use `pop_canvas_can_hot_swap`. Never assign `None`. Hot-swap highlight only. Defer full redraw with `QTimer.singleShot(0, ...)`. Run `tests/unit/test_dsos_population.py`. |
| `group_grating_conditions()` / `select_best_dsos_condition()` | An 8-dir cutoff tags 6-dir protocols as SF. Ranking by max `|DSI|` or a 2 Hz veto hides real DS/OS. | Plot the `(bw, tf)` pairs that ran. Rank classified pairs by peak response. Run `tests/unit/test_grating_calc.py` and `tests/unit/test_dsos_threshold.py`. |
| `grating_computed_cache.pkl` | Old DSOS/SF tags survive a protocol change. | `grating_entry_needs_recompute()` must drop the row. Do not reuse it. |

## 2. Tests to run after a change

| If you change | Run |
|---|---|
| Vision ID / `get_cell_physics` | `test_get_cell_physics_vision_id_offset` |
| ACG / standard plots | `test_acg_includes_late_spike_trains` |
| Caches | `tests/unit/test_data_manager_cache.py` |
| Lazy STA | `test_lazy_sta_dict_*` in `test_physics_cache_unified.py` |
| EI load / stride | `tests/unit/test_vision_load_robustness.py` |
| EI View / Overlay combo | `tests/unit/test_ei_panel_view.py` |
| Selectors / UMAP first paint | `tests/unit/test_live_selectors.py` |
| Dataset switch / memory | `tests/unit/test_dataset_release.py` |
| Theme tokens | `test_theme_keys_match` |
| Feature blocks / prefilter | `tests/unit/test_raw_feature_blocks.py` `tests/unit/test_dynamic_clustering.py` |
| Tree sidebar rows | `tests/unit/test_tree_rows.py` `tests/integration/test_tree_operations.py` |
| Shortcuts / key handling | `tests/unit/test_keymap.py` (checks README too) |
| Grating grouping / DSI/OSI pick | `tests/unit/test_grating_calc.py` `tests/unit/test_dsos_threshold.py` |
| Population DS/OS polar / hot-swap | `tests/unit/test_dsos_population.py` |

Use `tmp_path` or `cache_cleared_data_manager` for any math test. A real
run folder can hold a warm `.pkl` and skip the code under test (Law 3).

The full suite still has older failures. Do not mark them skipped.

## 3. Open defects

| Defect | Effect | Status |
|---|---|---|
| Mixed no-STA cells share one temporal PCA point | Fake tight UMAP cluster | Fixed 2026-08-12. PCA fits only cells that have the block; missing rows are NaN. UMAP uses observed Euclidean (shared features only, no MCAR scale-up). Cells stay in the map. |
| Default weights: STA block ~400 vs ACG ~4 | Embedding is almost STA-only | Fixed 2026-08-12. Defaults are Temporal + ACG + RF diameter, each 10/10, grating and chirp off. Euclidean share is still `n_columns × weight²`. |
| EI View combo stolen by heatmap redraws | Combo said Waveform; canvas showed Heatmap | Fixed 2026-08-12. Shared handlers go through `_redraw_current_view`. Wheel on a closed combo is ignored. |
| Stale `feature_cache.pkl` with `_computed: True` and `timecourse=None` | Population panel reports no timecourses | Fixed 2026-08-12. `_physics_entry_is_fresh` recomputes once when an STA source appears (`_sta_checked`). |
| DS/OS slider writes `dsos_threshold` but grating panel uses 0.3 | Slider does not change the grating label | Fixed 2026-08-12. User-checked 2026-08-24 on a 6-dir-per-condition grating run. |
| 6-dir `(bw, tf)` tagged SF; best run was max `|DSI|` | OSI/DSI missing; wrong condition shown | Fixed 2026-08-24. `MIN_DIRECTIONS_FOR_DSOS = 4`. Rank by peak response. |
| Grating batch after physics; 1000 shuffles | Second load wait | Fixed 2026-08-24. Batch starts with physics. 200 shuffles. Skip shuffle below 0.10. |
| `_pop_plot_state = None` plus nested mosaic draw | `AttributeError` and recursive QWidget repaint on cluster click | Fixed 2026-08-24. State is a dict or absent. Highlight-only hot-swap. |
| `get_cell_physics()` indexes the full STA cube | Slow scroll with a cold cache | Fixed 2026-08-12. Params timecourse first. Cube only on a miss. |
| `_draw_plots()` redraws population panels on every selection | Chirp-view scroll is slow until cache is warm | Fixed 2026-08-12. Skip when the group timecourse and ACG caches already hold the subset. First visit of a group still draws. |
| Older pytest failures | Suite is not a clean gate | Fixed 2026-09-24. The 3 `test_gui_polish.py` failures were stale tests (UMAP tab index 3 is now Grating; `QMouseEvent` needs `QPointF` in PyQt6). `requirements-dev.txt` (pytest, pytest-qt, pytest-mock) exists. Green on Python 3.10 (conda `rgcviewer`: 498 passed, 2 skipped) and 3.13 (pandas 3, numpy 2.5, no torch: 497 passed, 3 skipped). Still true: the suite does not load a real run; use the harness for that. |
| Grating angles are the protocol's label, not the motion | A grating labelled θ drifts its bars toward θ + 180°, so the Grating tab's polar lobe and the RF-mosaic DS arrows (`population_panel.py`, the arrow at (cos θ, sin θ) on the y-up mosaic) point opposite to the preferred motion | Found 2026-09-25 from the code (not yet checked on the rig): `GratingDSOS.m` passes θ to Stage unchanged (lines 120, 219); Stage `Grating.m` rotates anticlockwise by θ on a y-up canvas and samples s = phase/360 + n(x′+1)/2, so as the phase grows the crests move to −x′. The white-noise frame and Vision's y0 share the canvas's orientation (`TextureObject.m` flipdim, `Image.m`), so the relation holds on the screen and on the RF mosaic whatever the optics do. **Needs the lab's decision:** relabel Encore's DS angles as motion (θ + 180°; every past plot turns by 180°), or keep the label and say so. Check on the rig: θ = 0 should drift left. Compare DS Runs (Q51) already uses the motion direction and says so. |

## 4. Expected messages (not defects)

Leave these. Do not treat them as crashes.

| Message | Meaning | Action |
|---|---|---|
| STA provenance dialog; "N of M cells in the .sta do not exist in this sort" | The `.sta` is from an older sort | Use the noise-run STA or Map Reference |
| `ei=519, positions=512` in the EI panel | Converter wrote a mismatched EI | Leave the plot blank |
| `standard_plot_cache.pkl` discarded (too large / unreadable) | Cache file is stale | Next load rebuilds it |
| `retinanalysis` import skipped | Optional package is absent | Ignore |
| `PeakPropertyWindow` warning | scipy peak finder | Ignore |

## 5. Parked work

Do not start these unless the user asks.

| Item | Spec | Notes |
|---|---|---|
| UX / UI redesign | `docs/specs/ux_ui_redesign.md` | Spec only for later phases (browser, command palette, undo, auto-collapse, min 1100×650). Colors are the locked palette in `docs/design/palette.md`. Swiss 40px header (ENCORE + run meta + inline tabs) is in. Do not add the 1100×650 minimum. |
| Cross-run stimulus bridge lab acceptance | `docs/specs/cross_run_stimulus_bridge.md` | Code is in the tree. Lab AC is open. |
| Vision-only remaining gaps | `docs/specs/vision_standalone.md` | Missing `.sta` / `.params` no longer crash. |
| EI panel waveform view | none | View combo exists. Further waveform work is not started. |
| Desktop launcher / `update.sh` | none | Not started. |
| Firing-rate / burstiness embedding feature | none | Do not dump mean rate as a scalar. Needs a construction that separates high-baseline RGCs and bursty vs tonic firing (retina and, later, brain neurons) without letting spike count dominate as QC. ACG already carries some of this. Not started. |
| 3D UMAP view | none | The current 3D UMAP display is poor UX. A 3D embedding (or another embedding) may still be better *for clustering* than the 2D view; do not throw the extra dimension away without checking that. Not started. |
| Stimulus / epoch rasters | none | Moved to Q38 for the no-raw case. Chirp already has trial rasters. Grating and Contrast do not (grating spec chose a preferred-direction PSTH instead). Want rasters in more places: per-stimulus tabs and/or a dedicated raster surface. One proposal is to put a spike raster in the Raw tab so a run with no `.bin` (Vision-only, or raw disabled) still has a time view. Open design: keep rasters next to the stimulus they align to, vs one Raster page, vs a Raw fallback when voltage is missing. Do not start until that is decided. |

## 6. Commands

```bash
conda activate encore
python main.py
python -m pytest tests/unit/ -v
python -m pytest tests/unit/test_live_selectors.py tests/unit/test_vision_load_robustness.py tests/unit/test_ei_panel_view.py -v
```

On the lab workstation the working environment is conda `rgcviewer`.
User installs run from `~/.encore/.venv`.

```bash
# Unit suite, headless
QT_QPA_PLATFORM=offscreen conda run -n rgcviewer python -m pytest tests/unit -q

# Drive the real GUI offscreen on a real run; screenshots in $HARNESS_OUT
HARNESS_OUT=/tmp/encore_harness conda run --no-capture-output -n rgcviewer \
    python tools/gui_harness.py sta_refresh
# Same, with the raw .bin attached (FeatureWorker path)
HARNESS_DAT=/mnt/lab/Array-data/raw/20251212A/data018 \
    conda run --no-capture-output -n rgcviewer python tools/gui_harness.py sta_refresh
```

Harness dataset: `/mnt/lab/Array-data/sorted/20251212A/kilosort40/data018/ksfiles`
(Kilosort; Vision files one level up; DSOS grating file). Raw:
`/mnt/lab/Array-data/raw/20251212A/data018`. Its `.sta`/`.params` come from
a different sort (Q32): use it to test mechanics, not what an STA or RF
shows. Matched run (70 s cold load): pass
`/mnt/lab/Array-data/sorted/20260220A/kilosort25/data022/ksfiles` as the
second argument.

Scenario `params_save` copies the `.params` to `$HARNESS_OUT/params_save`
and points Encore at the copy. It never writes the lab file and checks
that its stamp did not change. It checks the result with Vision.jar when
one is found (`VISION_JAR`, default
`~/Documents/Development/MEA-fieldlab/src/vision7_symphony/Vision.jar`).

Lab data (tests skip if unmounted):

```
Raw Litke:     /mnt/lab/Array-data/raw/20260506A/data009
Sorted/Vision: /mnt/lab/Array-data/sorted/20260506A/chunk10/kilosort2.5
```
